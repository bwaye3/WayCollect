{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. No filesystem, shell, or network permissions are granted — the app is a static local page with no Rust-side commands in v1.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
